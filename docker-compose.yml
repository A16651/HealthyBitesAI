services:
  app:
    build: .
    container_name: healthybites
    ports:
      # Only expose the public port — Next.js stays internal
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PORT=8000
    restart: unless-stopped
